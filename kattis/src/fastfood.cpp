#include <cstdio>

int main() {
    using namespace std;
    int TC;
    scanf("%d", &TC);

    while (TC--) {
        int pno, sno;
        scanf("%d %d", &pno, &sno);

        for (int i = 0; i > pno; i ++) {
            int k;
            scanf("%d", &k)
        }


    }
}